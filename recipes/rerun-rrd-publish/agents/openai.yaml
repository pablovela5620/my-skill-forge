interface:
  display_name: "Rerun rrd publish"
  short_description: "Catalog segment or .rrd + .rbl -> one standalone .rrd on Hugging Face, checked in the web viewer."
  default_prompt: "Use $rerun-rrd-publish: export the segment (scripts/fetch_segment.py), join it with its blueprint and clip it (scripts/join_trim.py), upload with hf, then open the app.rerun.io link in playwright and inspect the screenshot before reporting."
