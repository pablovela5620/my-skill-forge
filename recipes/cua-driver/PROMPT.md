Keep this release matched to ai-demos/recipes/cua-driver and update both fleet
pins together. Use the upstream release skill archive and preserve all its
metadata and companion guides. Keep fleet.md as the minimal local adaptation.
Do not use generic latest-release autobump: this monorepo mixes components and
marks stable driver tags as prereleases. Update the paired packages explicitly.
