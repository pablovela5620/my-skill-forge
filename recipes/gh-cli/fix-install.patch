--- a/skills/gh-cli/SKILL.md
+++ b/skills/gh-cli/SKILL.md
@@ -14,6 +14,11 @@ Comprehensive reference for GitHub CLI (gh) - work seamlessly with GitHub from t
 
 ```bash
+# Preferred: install globally with pixi
+pixi global install gh
+
+# Fallbacks if pixi is unavailable:
+
 # macOS
 brew install gh
 
